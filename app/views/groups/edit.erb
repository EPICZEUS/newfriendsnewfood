<%= form_for @group do |f| %>
  <%= f.label :name, "Name" %>
  <%= f.text_field :name %>
  <% @group.user_ids.each do |id| %>
  	<%= hidden_field_tag :'group[user_ids][]', id %>
  <% end %>
  <%= f.submit "Update" %>
<% end %>