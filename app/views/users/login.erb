<%= form_for @user, action: "logging", method: :get do |f| %>
	<%= render 'shared/errors' %>
  <%= f.label :username, "Username" %>:
  <%= f.text_field :username %><br>
  <%= f.label :password, "Password" %>:
  <%= f.password_field :password %><br>
  <%= f.submit "Login" %>
<% end %>